<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('./partials/head.ejs', {title: 'My Profile' }) %>
</head>

<body>
    <%- include('./partials/header.ejs') %>
    
        <main class="py-[250px] bg-[url('/assets/neutral.jpeg')] bg-no-repeat bg-cover">
            <div class="max-w-[1200px] my-auto mx-auto">
                <div class=" gap-4 flex flex-col justify-center items-center">
                <h2 class="text-2xl">Welcome to your Nova profile, <%=employee.username.charAt(0).toUpperCase() +
                        employee.username.slice(1).toLowerCase()%>
                </h2>
                <p class="text-xl">FirstName: <%= employee.firstName.charAt(0).toUpperCase() +
                        employee.firstName.slice(1).toLowerCase()%>
                </p>
                <p class="text-xl">LastName: <%= employee.lastName.charAt(0).toUpperCase() + employee.lastName.slice(1).toLowerCase()%>
                </p>
                <p class="text-xl" >Department: <%= employee.department%>
                </p>
                <p class="text-xl">Position: <%= employee.position%>
                </p>
                <p class="text-xl">Email: <%= employee.email%>
                </p>
                <p class="text-xl">Phone number: <%= employee.phoneNumber%>
                </p>
                <p class="text-xl"> <img class="rounded-full w-40 h-40 object-cover" src="<%=employee.picture%>" alt="Employee image"></p>
            </div>
            </div>
        </main>
        <%- include('./partials/footer.ejs') %>
</body>

</html>