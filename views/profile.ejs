<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('./partials/head.ejs', {title: 'My Profile' }) %>
</head>

<body>
    <%- include('./partials/header.ejs') %>
    
        <main class="py-[250px] bg-[url('/assets/neutral.jpeg')] bg-no-repeat bg-cover">
            <div class="max-w-[1200px] my-auto mx-auto">
                <div class=" gap-4 flex flex-col justify-center items-center">
                <h2 class="font-bold text-2xl">Welcome to your Nova profile, <%=employee.username.charAt(0).toUpperCase() +
                        employee.username.slice(1).toLowerCase()%>!
                </h2>
                <p class="text-xl"><span class="font-bold">FirstName:</span> <%= employee.firstName.charAt(0).toUpperCase() +
                        employee.firstName.slice(1).toLowerCase()%>
                </p>
                <p class="text-xl"><span class="font-bold">LastName: </span><%= employee.lastName.charAt(0).toUpperCase() + employee.lastName.slice(1).toLowerCase()%>
                </p>
                <p class="text-xl"><span class="font-bold">Department:</span> <%= employee.department%>
                </p>
                <p class="text-xl"><span class="font-bold">Position: </span><%= employee.position%>
                </p>
                <p class="text-xl"><span class="font-bold">Email:</span> <%= employee.email%>
                </p>
                <p class="text-xl"><span class="font-bold">Phone number:</span> <%= employee.phoneNumber%>
                </p>
                <p class="text-xl"> <img class="rounded-full w-40 h-40 object-cover" src="<%=employee.picture%>" alt="Employee image"></p>
            </div>
            </div>
        </main>
        <%- include('./partials/footer.ejs') %>
</body>

</html>